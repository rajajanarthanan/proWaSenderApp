name: simplyfirescore
description: "SimplyFires Base"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router:
  bloc:
  freezed:
  build_runner:
  dartz:
  json_serializable:
  logger:
  json_annotation:
  freezed_annotation:
  equatable:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/